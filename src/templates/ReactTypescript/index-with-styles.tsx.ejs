import React from 'react';

import { Container } from './styles';

const <%= props.name %>: React.FC = () => {
  return (
    <Container>
      <h1><%= props.name %></h1>
    </Container>
  );
};

export default <%= props.name %>;
